<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Welcome to Aby HR</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <style>
    /* Email-safe simple styles */
    body { font-family: Arial, sans-serif; background:#f4f6f9; margin:0; padding:0; -webkit-text-size-adjust:100%; }
    .container { max-width:600px; margin:20px auto; background:#fff; border-radius:8px; padding:20px; box-shadow:0 2px 8px rgba(0,0,0,0.06); }
    h1 { color:#1f8f7a; font-size:22px; margin:0 0 10px; }
    p { font-size:15px; color:#333; line-height:1.4; margin:10px 0; }
    .info { margin:15px 0; padding:12px; background:#f1f9f6; border:1px solid #d7f0ea; border-radius:6px; }
    .info p { margin:6px 0; font-size:15px; }
    .cta { display:block; text-decoration:none; padding:12px 18px; border-radius:6px; background:#1f8f7a; color:#fff; font-weight:600; text-align:center; margin:16px 0; }
    .small { font-size:13px; color:#777; }
    .footer { text-align:center; font-size:13px; color:#999; margin-top:18px; }
    /* Make button display on mobile nicely */
    @media only screen and (max-width:480px) {
      .container { padding:14px; }
      h1 { font-size:20px; }
    }
  </style>
</head>
<body>
  <!-- Preheader text (visible in inbox preview) -->
  <div style="display:none; max-height:0; overflow:hidden; font-size:1px; line-height:1px; color:#fff; opacity:0;">Your Aby HR login details are inside — log in and change your password.</div>

  <div class="container">
    <h1>Welcome, {{firstname}} {{lastname}} 🎉</h1>

    <p>You have been registered in the <strong>Aby HR</strong> system. Use the details below to sign in:</p>

    <div class="info" role="presentation">
      <p><strong>Email:</strong> {{email}}</p>
      <p><strong>Password:</strong> {{password}}</p>
    </div>

    <a href="{{loginUrl}}" class="cta" target="_blank" rel="noopener noreferrer">Log in to Aby HR</a>

    <p class="small">Tip: For your security, consider changing this password after your first login.</p>

    <div class="footer">
      <p>© {{year}} Aby HR | All rights reserved.</p>
    </div>
  </div>
</body>
</html>
